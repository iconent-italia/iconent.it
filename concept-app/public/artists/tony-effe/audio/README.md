# Audio — Tony Effe

Drop the loop here with this EXACT name (config `config/artists/tony-effe.js` → `music.audioLoop` points to it):

- `icon-loop.mp3` — 20–40s seamless instrumental/bassline loop, ~-14 LUFS, ~128kbps.

Until the file lands, `SoundToggle` still renders and the Music-panel `EqBars` show an idle
baseline; pressing SOUND is a no-op (404 is caught). The whole audio system activates
automatically the moment this file exists.
